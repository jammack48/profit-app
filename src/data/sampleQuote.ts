import { QuoteData } from '../types/quote';

// Empty quote data - calculator will be empty until screenshot is imported
export const sampleQuoteData: QuoteData = {
  gstRate: 0.15,
  lineItems: []
};